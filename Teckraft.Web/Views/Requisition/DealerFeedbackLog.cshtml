    <div class="row pnl-page-detail-action">
        <div class="col-md-12">
            <h1>Comments Log</h1>
        </div>
    </div>
<!-- Main content -->
<section class="content">
    <div class="table-responsive">
        <table ng-hide="logDetails.length==0" class="table table-hover table-striped table-bordered">
            <thead>
                <tr>
                    <th>Sr. No.</th>
                    <th>Comments</th>
                    <th>Commented By</th>
                    <th>Created Date</th>
                </tr>
            </thead>
            <tbody>
                <tr data-ng-repeat="c in logDetails">
                    <td>{{$index + 1}}</td>
                    <td>{{c.comments}}</td>
                    <td>{{c.rcb.title}}</td>
                    @*<td>{{c.rct | date: 'dd-MMM-yyyy'}}</td>*@
                    <td>{{c.rct}}</td>
                </tr>
            </tbody>
    </table>
    </div>
            @*    <div ng-show="ihLog.length==0" class="alert alert-danger">
                    <i class="fa fa-ban"></i>No Records Found
                </div>*@
</section>
<!-- /.content -->
